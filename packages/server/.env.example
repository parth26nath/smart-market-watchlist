# Copy to .env — SQLite needs no further setup.
DATABASE_URL="file:./dev.db"
PORT=4000
SESSION_COOKIE_NAME=watchlist_session
# "fake" (default, deterministic, offline) or "finnhub" (real, needs FINNHUB_API_KEY)
MARKET_DATA_PROVIDER=fake
FINNHUB_API_KEY=
WEB_ORIGIN=http://localhost:5173
